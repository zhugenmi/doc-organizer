# {{系统/模块名称}}：架构设计

> 状态：Draft | Active | Superseded　　日期：YYYY-MM-DD　　维护人：{{owner}}

> 这篇文档回答"系统长什么样、怎么跑、用了什么"。半年后新工程师读完应能独立定位问题；决策原因不写这里，写 `adr/`。

## 背景与目标

> 一段话讲清楚：这个系统/模块解决什么问题、服务谁、不解决什么。范围边界写明白，避免日后文档膨胀。

{{背景与目标}}

**非目标**：{{明确不做什么，避免范围蔓延}}

## 模块划分

> 系统由哪些模块组成，每个模块的职责与边界。这是"模块设计"的核心--如果两个模块职责重叠，说明划分有问题。

| 模块 | 职责（一句话） | 对外接口 | 依赖 |
|------|---------------|---------|------|
| {{模块 A}} | {{做什么}} | {{API/事件/文件}} | {{依赖哪些模块}} |
| {{模块 B}} | {{做什么}} | {{API/事件/文件}} | {{依赖哪些模块}} |

**关键边界**：
- {{模块 A}} 与 {{模块 B}} 的边界：{{怎么划分的，为什么这样划}}
- {{模块 B}} 与 {{模块 C}} 的边界：{{同上}}

> 边界写不清楚的模块，代码里一定会互相渗透。这里花时间值得。

## 运行机制

> 系统实际怎么跑。这是"运行机制"的核心--读完这节，应能回答"一个请求/任务从进入到完成经历了什么、哪里可能卡住、失败时怎么办"。

### 生命周期 / 启动与停止

> 服务/任务怎么启动、怎么优雅停止。启动顺序、健康检查、退出时的清理。

{{生命周期描述}}

### 请求/数据流

> 一个典型请求或数据单元从入口到出口的完整路径。用文字或 ASCII 图描述。

```
{{入口}} -> {{模块 A}} -> {{模块 B}} -> {{存储/外部服务}} -> {{模块 C}} -> {{出口}}
```

**关键流转点**：
- {{流转点 1}}：{{发生了什么转换、为什么}}
- {{流转点 2}}：{{同上}}

### 状态机 / 状态转换

> 如果系统有显式状态（订单状态、任务状态、会话状态），画出来。状态机文档化能避免大量 off-by-one 的状态 bug。

```
[初始态] --事件A--> [中间态] --事件B--> [终态]
                      |
                      +--事件C--> [异常态]
```

### 并发与同步

> 哪里有并发、用什么机制保护（锁/CAS/队列/事务）、并发度限制。并发模型写明白，后续排查竞态有据可依。

{{并发模型}}

### 失败与重试

> 哪些环节会失败、失败时系统的行为（重试/降级/熔断/死信）。不要写"我们会重试"这种废话--写重试几次、间隔多久、什么时候放弃。

{{失败处理策略}}

## 接口契约

> 对外接口的关键约定。完整契约放 `arch/api/`，这里只写理解架构所需的核心契约。

- **{{接口 1}}**：{{输入 -> 输出，一句话}}。详见 `arch/api/...`。
- **{{接口 2}}**：{{同上}}。

**版本与兼容**：{{当前版本、兼容性策略}}

## 技术栈

> 当前实际使用的技术栈总览（living doc）。选型原因不写这里，写 `adr/` 并在此链接。

| 层 | 技术 | 版本 | 选型 ADR |
|----|------|------|---------|
| 语言 | {{Python 3.11}} | - | - |
| Web 框架 | {{FastAPI}} | {{0.110}} | [`adr/0003-chose-fastapi.md`](../adr/0003-chose-fastapi.md) |
| 前端 | {{React 18}} | - | [`adr/0005-chose-react.md`](../adr/0005-chose-react.md) |
| 数据库 | {{PostgreSQL 15}} | - | [`adr/0007-chose-postgres.md`](../adr/0007-chose-postgres.md) |
| 编排 | {{LangGraph}} | - | [`adr/0011-chose-langgraph.md`](../adr/0011-chose-langgraph.md) |

> 技术栈变动时更新此表，并在 `adr/` 留下决策记录。表格保持简洁--细节在 ADR 里。

## 部署拓扑（可选）

> 系统部署在什么上、组件间网络关系。仅当部署形态影响架构理解时写。

```
{{部署拓扑示意}}
```

## 已知限制与风险

> 当前架构的已知短板。诚实写出来，让后续优化有据可依，而不是每次都重新发现。

- {{限制 1}}：{{影响 / 缓解措施 / 是否计划解决}}
- {{限制 2}}：{{同上}}

## 相关

- 架构决策：`docs/adr/000X-*.md` - 本文档涉及的关键选型原因
- 子模块设计：`docs/arch/.../` - 各模块的详细设计
- 运行监控：`docs/monitor/YYYY-MM-DD-*.md` - 观察本系统运行状态的指标
- 故障应急：`docs/runbooks/YYYY-MM-DD-*.md` - 本系统故障时的处理手册
